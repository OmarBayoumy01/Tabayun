# Base URL of the AI Image Detector backend.
# The endpoints (/auth/login, /detect/image, /detect/video, /feedback, /health)
# are served from here. Copy this file to .env and adjust as needed.
VITE_API_URL=
